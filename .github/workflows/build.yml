name: Build iOS App

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Xcode
        run: sudo xcode-select -s /Applications/Xcode.app || true

      - name: Install XcodeGen
        run: brew install XcodeGen

      - name: Generate project
        run: xcodegen generate

      - name: Build for iOS Device
        run: |
          xcodebuild -project AdhkarWidget.xcodeproj \
            -scheme AdhkarWidget \
            -destination 'generic/platform=iOS' \
            -configuration Debug \
            -derivedDataPath /tmp/AdhkarWidgetBuild \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Apply ad-hoc signature and create IPA
        run: |
          APP="/tmp/AdhkarWidgetBuild/Build/Products/Debug-iphoneos/AdhkarWidget.app"
          codesign --force --sign - --deep "$APP" || true
          # Also sign the extension
          EXT="$APP/PlugIns/AdhkarWidgetExtension.appex"
          if [ -d "$EXT" ]; then
            codesign --force --sign - "$EXT" || true
          fi
          mkdir -p /tmp/Payload
          cp -R "$APP" /tmp/Payload/
          cd /tmp
          zip -r AdhkarWidget.ipa Payload -x "*.DS_Store"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: AdhkarWidget.ipa
          path: /tmp/AdhkarWidget.ipa
